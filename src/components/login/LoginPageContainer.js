import React, { Component } from 'react';
import LoginPage from './LoginPage.js'

class LoginPageContainer extends Component {
  render() {
    return (
        <LoginPage/>
    );
  }
}

export default LoginPageContainer; // Don’t forget to use export default!